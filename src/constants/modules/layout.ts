/* 统一策略 （控制使用 debounce 还是 throttle） */
export const STRATEGY: 'debounce' | 'throttle' = 'debounce'

/* 是否记住的滚动条距离 */
export const REMEMBER_SCROLLBAR_TOP: boolean = true

/* 统一触发间隔 */
export const INTERVAL: number = 300
