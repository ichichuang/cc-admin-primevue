/** 路由配置模块语言配置 */
export interface RouterLocaleMessages {
  core: {
    root: string
    login: string
  }
  dashboard: {
    dashboard: string
  }
  example: {
    example: string
    screen: string
    fullscreen: string
  }
  error: {
    notFound: string
    forbidden: string
    serverError: string
  }
}
