/** 路由配置模块语言配置 */
export interface RouterLocaleMessages {
  core: {
    root: string
    login: string
  }
  dashboard: {
    dashboard: string
  }
  example: {
    example: string
    animate: string
  }
  error: {
    notFound: string
    forbidden: string
    serverError: string
  }
}
